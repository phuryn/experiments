Audit the markdown documentation in the `documentation/` folder against the actual code in this repository. Find every claim in the docs that is stale or wrong — i.e. a statement the current code contradicts (for example: a wrong function or member name, a wrong table name, a wrong default value, a wrong route path, or a documented feature that does not actually exist in the code).

For each stale/wrong claim you find, report: the documentation `file:line`, a quote of the wrong claim, what the code actually says, and the contradicting code `file:line`. Be precise and exhaustive.
